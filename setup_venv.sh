# run these commands to setup a mini environment for this demo
python3 -m venv venv
source venv/bin/activate

# install all the reqs, you can do this either in the virtual environment or in your global install
pip install -r requirements.txt